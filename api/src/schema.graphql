scalar Point
scalar DateTime
scalar PointInput

type User {
  email: String
  tabs: Tab!
}

type Tab{
  tabId: Int
  user: [User!]@relationship(type: "TAB", direction: IN)
  split: Split!
}

type Split{
  data: String
  tab: [Tab!]@relationship(type: "SPLIT", direction: IN)
}

